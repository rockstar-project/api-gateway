spring:
  profiles: docker
server:
  port: 8765
  context-path: /v1
zuul:
  routes:
    metadata:
      path: /metadata/**
      service-id: coreapi-metadata
      strip-prefix: false
      sensitiveHeaders:
    templates:
      path: /products/**
      service-id: coreapi-products
      strip-prefix: false
      sensitiveHeaders:
    artifacts:
      path: /artifacts/**
      service-id: coreapi-artifact
      strip-prefix: false
      sensitiveHeaders:
proxy:
  auth:
    routes:
      metadata: passthru
      templates: passthru
      artifacts: passthru
security:
  basic:
    enabled: false
logging:
  level:
    org.springframework.security.oauth2: DEBUG
eureka:
  instance:
    prefer-ip-address: true
    leaseRenewalIntervalInSeconds: 10
    statusPageUrlPath: /info
    healthCheckUrlPath: /health
  client:
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/